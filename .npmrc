# .npmrc
engine-strict=true
save-exact=true
package-lock=true
legacy-peer-deps=false

# Security: Prevent scripts during installation
ignore-scripts=false

# Registry
registry=https://registry.npmjs.org/